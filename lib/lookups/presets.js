/* jshint -W097 */
/* globals module */
'use strict';

/**
 * Server default and preset values
 */
var Presets = {
    serverListenPort: 21000,
    serverAddress: 'localhost',
    timeToNextPingMilliseconds: 10000,
};

module.exports = Presets;
